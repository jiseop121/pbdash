interface:
  display_name: "Release Brew"
  short_description: "Release pbdash Homebrew artifacts and verify brew install."
  default_prompt: "Use $release-brew to publish a pbdash release, update Formula/pbdash.rb, and verify Homebrew installation."
